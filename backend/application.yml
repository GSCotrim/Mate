spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mate
    username: mate
    password: mate
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
