services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mate
      POSTGRES_USER: mate
      POSTGRES_PASSWORD: mate
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
